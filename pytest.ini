[pytest]
testpaths = test

markers =
    integration: tests that use the real local knowledge base, SQLite, Qdrant, filesystem, or multiple production components
    llm: tests that make real external LLM API calls
    slow: tests that may load embedding models, perform extraction, indexing, or other expensive operations